apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-exemple.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-exemple.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-exemple.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
